# ML Training Configuration
data_dir: "data/synthetic"
epochs: 100
batch_size: 8
lr: 0.001
model_type: "pointnet++"  # or "kpconv"
num_points: 8192
num_classes: 20
in_channels: 6
weight_decay: 0.0001
num_workers: 4
checkpoint_dir: "ml/checkpoints"

# Building extraction config
building_ext:
  patch_size: 256
  in_channels: 4  # DSM + RGB
  batch_size: 16
  lr: 0.001
  epochs: 100